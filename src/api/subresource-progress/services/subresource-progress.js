'use strict';

/**
 * subresource-progress service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::subresource-progress.subresource-progress');
